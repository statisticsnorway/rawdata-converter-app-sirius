micronaut:
  application.name: rawdata-converter-app-sirius
  server:
    port: 18080

services:
  dapla-oauth:
    token-provider: mock
  dapla-data-access:
    impl: MOCK
  dapla-metadata-distributor:
    impl: MOCK

